[build]
  # This tells Netlify: "My website files are in the current folder, not 'public'"
  publish = "." 
  functions = "netlify/functions"

[dev]
  functions = "netlify/functions"

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200